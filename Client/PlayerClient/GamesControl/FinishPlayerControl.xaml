<UserControl x:Class="Client.PlayerClient.GamesControl.FinishPlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.PlayerClient.GamesControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0">
                <TextBlock x:Name="txtQuestion"/>
            </Label>
            <Image x:Name="image" Grid.Column="1"/>
        </Grid>
        <Button x:Name="btnBell" IsEnabled="False" Grid.Row="1"  Click="btnBell_Click" Background="Red" Foreground="White" FontWeight="Bold">Giành quyền trả lời</Button>
    </Grid>
</UserControl>
